---
linter:
  rubocop:
    rails: true
    # Inherit from a given version of standard.rb
    # https://help.sider.review/tools/ruby/rubocop#gems
    gems:
      - name: 'rubocop'
        version: '0.67.2'
      - name: 'standard'
        version: '0.0.37'
